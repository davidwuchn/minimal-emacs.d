λ(command). Bash | sandbox:plan-mode

## Availability
- `Bash`: :core, :readonly, :researcher, :nucleus, :snippets

## Parameters
- `command` (string): Bash command

## Plan Mode Sandbox
Read-only whitelist: git/ls/cat/grep/find/test runners. ¬{>,<,sed -i,&,$()}.
