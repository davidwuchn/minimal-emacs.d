## Research Focus: Minimal Pair Probes

For each bug or pattern found, produce TWO competing hypotheses that
differ in exactly ONE aspect (different guard position, different
error handler style, different default value). Label them HA and HB.
This lets us A/B test which approach works better.
Prefix each finding with: `[pair-probe]`
