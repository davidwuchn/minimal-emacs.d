# Insight: 2026-05-04 04:20

**Target:** lisp/modules/gptel-tools-agent-git.el
**Change type:** safety
**Hypothesis:** Replacing the character-by-character `mapconcat` approach in `my/gptel--xml-escape` with chained `replace-regexp-in-string` calls improves performance and fixes a latent bug where the original code escaped `&` first, causing double-escaping of other entities. The corrected order (`&` first, then others) ensures proper XML escaping.
**Decision:** nil

Unexpected experiment outcome.

---
*Generated by auto-workflow*
