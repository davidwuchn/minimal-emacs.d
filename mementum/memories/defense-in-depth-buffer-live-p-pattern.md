When writing callbacks passed to `gptel-auto-workflow--iterate-project-buffers`, add defense-in-depth `buffer-live-p` guard even though the iterator checks it. The callback lambda is a reusable closure that could be stored or passed elsewhere. Also wrap fragile operations like `format-mode-line` in `ignore-errors` for resilience against batch/non-graphical modes. This pattern improves Vitality (error recovery) with minimal code surface area.