When iterating over buffers in async Emacs workflows, always add a secondary `buffer-live-p` guard at the use site (not just the iterator). Buffers can be killed between the iterator's `buffer-live-p` check and the lambda's `with-current-buffer` call, causing "Selecting deleted buffer" errors. Pattern applied in `gptel-auto-workflow-list-project-buffers` — defense in depth for TOCTOU race conditions.