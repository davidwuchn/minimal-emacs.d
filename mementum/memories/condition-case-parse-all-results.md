Guard `gptel-auto-workflow--parse-all-results` with `condition-case nil ... (error nil)` when the function handles graceful degradation (empty list return). This prevents errors from results file corruption or missing experiment directories from propagating up and breaking dispatch. Several callers already wrap this, but the callee `gptel-auto-workflow--backend-per-axis-keep-rates` was missing this guard.