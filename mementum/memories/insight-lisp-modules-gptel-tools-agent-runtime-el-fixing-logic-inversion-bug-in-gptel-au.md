# Insight: 2026-05-02 05:01

**Target:** lisp/modules/gptel-tools-agent-runtime.el
**Change type:** bug-fix
**Hypothesis:** Fixing logic inversion bug in `gptel-auto-workflow--seed-worktree-runtime-var` where `unless (not (file-directory-p ...))` should be `when (file-directory-p ...)`. The confusing double-negation made the condition backwards - it set `canonical-root` when var did NOT exist under base-root, when the intent was to set it when var DOES exist.
**Decision:** nil

Unexpected experiment outcome.

---
*Generated by auto-workflow*
