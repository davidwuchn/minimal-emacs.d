# Insight: 2026-05-08 08:22

**Target:** lisp/modules/gptel-ext-tool-sanitize.el
**Change type:** bug-fix
**Hypothesis:** The `my/gptel--nil-tool-call-p` function had incomplete validation for args - it used `listp` which returns t for dotted pairs like `(a . b)`, but these are improper lists that would cause downstream errors when accessed as plists. Changing to `proper-list-p` will improve Clarity by using a precise validation function and prevent runtime errors from malformed tool call args.
**Decision:** nil

Unexpected experiment outcome.

---
*Generated by auto-workflow*
