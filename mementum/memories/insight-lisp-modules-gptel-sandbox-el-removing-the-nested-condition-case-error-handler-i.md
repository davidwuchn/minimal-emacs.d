# Insight: 2026-05-01 15:47

**Target:** lisp/modules/gptel-sandbox.el
**Change type:** safety
**Hypothesis:** Removing the nested `condition-case` error handler in `gptel-sandbox--apply-builtin` will fix improper error propagation. The original code caught errors from `apply` and then signaled a new error, which created a nested error situation that could escape the intended handler scope. By removing the wrapper and letting errors propagate directly to the outer `condition-case` in `execute-tool`, errors are now properly caught and formatted with consistent error messages.
**Decision:** nil

Unexpected experiment outcome.

---
*Generated by auto-workflow*
