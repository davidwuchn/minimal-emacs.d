---
title: System Health Patterns
status: active
category: system-health
tags: [self-audit, auto-fix, meta]
related: [pipeline-health, self-healing-architecture]
last-updated: 2026-06-08T19:00:09
---

# System Health Patterns

Auto-generated from self-audit memories. Updated every pipeline
run when >=3 audit-fix memories exist.

## Summary (3 audit runs analyzed)

- **Backend cold-start**: detected in 3/3 runs (avg 8.0 cold backends)
- **Strategy cold-start**: detected in 3/3 runs (avg 29.0 unevaluated strategies)
- **Staging-merge bottleneck**: detected in 3/3 runs

## Recurring Root Causes

### Backend Cold-Start (>=3 occurrences)
- **Symptom**: 5-8 backends never tried within cold-start window
- **Impact**: Reduced model diversity, reliance on single provider
- **Auto-fix deployed**: force-try-backends.txt signal -> daemon unblocks rate-limited backends
- **Verification**: check `gptel-auto-workflow--rate-limited-backends` size after pipeline run
- **Status**: auto-fix applied each pipeline cycle

### Strategy Cold-Start (>=3 occurrences)
- **Symptom**: 12-15 strategies never evaluated (hidden by 25% Bayesian floor)
- **Impact**: Poor strategy selection, template-default bias
- **Auto-fix deployed**: exploration-rateOverride.txt signal -> daemon increases exploration rate
- **Verification**: check strategy pool visibility in digest
- **Status**: auto-fix applied each pipeline cycle

### Staging-Merge Bottleneck (>=3 occurrences)
- **Symptom**: git conflicts in .md files cause experiment failures
- **Impact**: 3-4%% keep-rate ceiling, wasted experiment cycles
- **Auto-fix deployed**: --try-autoresolve-conflicts uses --theirs for .md files
- **Limitation**: source code (.el) conflicts still require manual review
- **Status**: auto-fix active; .el conflicts escalated to human

## Actionability

These patterns are injected into the experiment prompt builder via `gptel-auto-experiment--system-health-for-prompt`, causing the evolution LLM to:
1. **Prioritize fixes** for targets contributing to known bottlenecks
2. **Avoid strategies** that reproduce cold-start patterns
3. **Self-monitor** by checking whether its own changes improve system health metrics

---

*Generated by gptel-auto-workflow-self-audit.el - Learning<-Quality loop*
