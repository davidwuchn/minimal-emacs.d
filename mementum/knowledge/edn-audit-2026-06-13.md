---
title: EDN Audit — Data Exchange Formats in OV5
status: active
category: architecture
tags: [edn, data, ov5, audit, migration]
related: [mementum/innovation-queue.edn, plans/ov5-world-store]
depends-on: []
---

# EDN Audit — Data Exchange Formats in OV5

Date: 2026-06-13
Scope: Runtime state, cross-daemon/pipeline data, persisted queues/metrics/decisions, and on-disk information exchange points in `~/.emacs.d`.

## Principle

OV5 should use **EDN** for all machine-to-machine data and info exchange. Markdown, plain text, JSON, JSONL, and ad-hoc sexp are tolerated only when:

- The artifact is primarily human-facing (reports, dashboards, logs).
- An external system requires the format (CI artifacts, RDF/Turtle).
- It is a scalar signal or lock file (PID, timestamp, heartbeat).

## Current state

| Status | Count | Examples |
|--------|-------|----------|
| Already EDN | 10+ | `mementum/innovation-queue.edn`, `bb.edn`, `var/tmp/cron/auto-workflow-status.edn`, `var/tmp/cross-subsystem-state.edn`, `var/context/*.edn`, `var/tmp/self-audit-result.edn`, `var/approval-queue/**/*.edn`, `var/risk-patterns.edn`, `var/approval-history.edn`, `var/disposable/*.edn`, `var/impact-assessments.edn`, `var/metrics/*-metrics.edn`, `var/tmp/researcher-feedback.edn`, `var/tmp/category-champions.edn`, `var/tmp/research-findings.edn` |
| High-priority migration candidates | 1 | `var/tmp/checkpoints/*.ckpt` |
| Medium-priority candidates | ~20 | JSON state files (`controller-evolution-history.json` already EDN; remaining strategy metadata JSON, digital-twin.json, target-state.json, etc.), ad-hoc `.eld`/`.el` snapshots, custom text indexes, JSONL logs, signal TXT files, markdown reports with tables |
| Keep as-is | many | Human markdown, logs, lock files, static JSON configs, RDF/Turtle |

## High-priority migration candidates

1. **`var/tmp/experiments/<run-id>/results.tsv`** → **COMPLETE (2026-06-14)** — migrated to Datahike World Store via `clj/ov5/world_store/`. Historical TSV files (117) backfilled; 59 entities in store. New experiments transacted directly to Datahike via `gptel-auto-experiment-log-tsv`. TSV parser (`gptel-auto-workflow--parse-all-results`) now queries World Store. Shell scripts (`run-pipeline.sh`, `check-evolution-status.sh`, `run-tests.sh`) updated. Historical TSVs preserved for audit.

2. **`var/tmp/checkpoints/active.ckpt`** → **COMPLETE (2026-06-14)** — migrated to EDN (`gptel-ext-checkpoint.el`). Active checkpoint is now `active.edn`; archived checkpoints use `.edn`. Legacy JSON `.ckpt` files are automatically converted on first load and renamed to `.ckpt.migrated`. Added `tests/test-gptel-ext-checkpoint.el` with serialization, save/load, archive, legacy migration, and empty-results roundtrip tests (5/5 pass).

## Completed migrations

### 2026-06-14: checkpoints.ckpt → EDN
- `gptel-ext-checkpoint.el` now writes/reads EDN via `parseedn-print-str` and shared `gptel-auto-workflow--read-edn` / `gptel-auto-workflow--edn-to-plist` helpers.
- File paths changed from `.ckpt` to `.edn` (`active.edn`, `history/<run-id>.edn`).
- Legacy JSON `.ckpt` files are loaded once via `gptel-checkpoint--load-legacy`, converted to EDN, saved as `active.edn`, and the original is renamed to `.ckpt.migrated`.
- Added `tests/test-gptel-ext-checkpoint.el`: serialization roundtrip, save/load, archive extension, legacy migration, empty-results roundtrip.
- Cleaned up stale-lock-p and snapshot-loop-state paren issues uncovered during testing.

### 2026-06-14: research-findings.md → EDN
- `gptel-auto-workflow--research-file` now returns `var/tmp/research-findings.edn`
- `gptel-auto-workflow-run-research` writes `{:project ... :updated ... :findings ...}` EDN
- `gptel-auto-workflow-load-research-findings` and `gptel-auto-workflow--read-research-findings-file` parse EDN and extract `:findings`
- `gptel-auto-workflow--research-fresh-enough-p` checks `:findings` content instead of line count
- `standalone-research.el` (`slr--save-findings`) writes compatible EDN
- Direct readers in `gptel-auto-workflow-evolution.el` parse EDN before pattern extraction
- Hardcoded `.md` paths updated in production.el, evolution.el, projects.el, standalone-research.el, tests, scripts, docs, assistant skills
- Active `var/tmp/research-findings.md` converted to `.edn`; stale `.md` worktree copies left for audit
- Tests updated: `regression/auto-workflow/load-research-findings-restores-context`, `regression/auto-workflow/load-research-findings-nil-cache-no-crash`

### 2026-06-14: results.tsv → Datahike World Store
- Removed TSV header, ensure-results-file (now no-op), and TSV parsing from `gptel-auto-workflow-evolution.el`
- `gptel-auto-experiment-log-tsv` now transacts via `ov5-world-store--brepl-eval`
- 117 historical TSV files backfilled (80 rows transacted, 59 valid entities)
- Shell scripts updated: run-pipeline.sh, check-evolution-status.sh, run-auto-workflow-cron.sh, run-tests.sh
- Extended Datahike base-schema: 53 experiment attributes + 11 gate score attributes
- Added `transact-experiment`, `experiments-by-decision-and-age`, `kept-experiment-count`, `kept-target-count` helpers

### 2026-06-13

- `var/tmp/self-audit-result.el` → `.edn` (shell consumer updated)
- `var/context/*.sexp` → `.edn` (context-db + world-store + production counts)
- `var/approval-queue/**/*.sexp` → `.edn` (approval-queue module + pipeline + prompt-build)
- `var/risk-patterns.sexp` + `var/approval-history.sexp` → `.edn`
- `var/disposable/*.sexp` → `.edn`
- `var/impact-assessments.sexp` + `var/metrics/*-metrics.sexp` → `.edn`
- `var/tmp/researcher-feedback.sexp` + `var/tmp/category-champions.sexp` → `.edn`
- Isolated temp status files and regression test fixtures updated to `.edn`

## Medium-priority migration candidates

- JSON state files: `controller-evolution-history.json`, `researcher-strategy.json`, `evolution-scores.json`, `digital-twin.json`, `target-state.json`, `context-savings.json`, `circuit-breaker-state.json`, `ab-results.json`, `skill-eval-opencode/results/*.json`, `pending_eval.json`, strategy metadata JSON.
- Ad-hoc sexp / `.eld` snapshots: `category-champions.sexp`, `researcher-feedback.sexp`, `knowledge-snapshot.el`, `backend-fallback-order.el`, `pipeline-statechart.eld`, `skill-graph.eld`, `prefix-cache-state.eld`, `savefile/gptel-context-window-cache.el`, `risk-patterns.sexp`, `approval-history.sexp`.
- Custom text indexes: `mementum/.index`, `mementum/.dedup-cache`, `mementum/.pipeline-log`.
- JSONL logs: `evaluations.jsonl`, `evolution_summary.jsonl`, `research-defects.jsonl`, `risk-node-training-pairs.jsonl`, `conversion-units.jsonl`.
- Signal TXT files: `force-try-backends.txt`, `exploration-rateOverride.txt`, `grader-timeoutOverride.txt`, `force-grader-backends.txt`.
- Markdown reports with tables: `batch-anchor-report.md`, `semantic-relationships.md`, `backend-comparison.md`, `model-comparison.md`, `pipeline-health.md`, `system-health-patterns.md`, `digests/*.md`.

## Keep as-is

- Human-facing Markdown: `mementum/state.md`, `mementum/memories/*.md`, `mementum/knowledge/**/*.md`, dashboards, static assistant skills/agents/commands/strategies, `benchmarks/README.md`.
- Plain text logs and lock files: `*.log`, `*.lock`, `watchdog-last-restart`, `cron-error.txt`, `daemon-heartbeat`, `alerts.log`.
- Static JSON configs/schemas: `eca/config.json`, `assistant/evals/*.json`, `assistant/schemas/example-benchmark.json`, skill/workflow test definitions.
- RDF/Turtle files: `*.ttl`.

## Next actions

1. Pick one high-priority candidate and migrate it end-to-end with tests.
2. Add a reusable `ov5-read-edn` / `ov5-write-edn` helper to reduce duplication.
3. Update shell consumers to use the helper or a small EDN→JSON bridge.
4. Re-audit after each migration until all high-priority candidates are EDN.
