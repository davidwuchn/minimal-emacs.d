# STATE

- **PLAN MODE SANDBOX ENFORCEMENT PROMOTED TO SERVER-SIDE**: DEFINED A NEW `BashRO` TOOL WITH A STRICT READ-ONLY DESCRIPTION FOR THE LLM. THIS REPLACES THE STANDARD `Bash` TOOL IN THE `:readonly` PRESET, ENSURING THE AGENT IS AWARE OF THE SANDBOX CONSTRAINTS BEFORE PLANNING, ELIMINATING WASTED TOKEN ATTEMPTS.
- **TOOL ARGUMENT SCHEMAS STANDARDIZED**: UNIFIED ALL `gptel-make-tool` ARGUMENT DEFINITIONS ACROSS THE CODEBASE TO USE LISP SYMBOLS FOR TYPES (E.G. `:type string` INSTEAD OF `:type "string"`) AND CONSISTENTLY USE STATIC QUOTED LISTS INSTEAD OF VERBOSE `(list '(:name ...))` CONSTRUCTS.
- **MATHEMATICAL TOOL PATTERNS ENFORCED**: TOOL USE GUIDELINES ACROSS ALL PROMPTS (E.G., `INIT_AGENTS.MD`, `CODE_AGENT.MD`, `PLAN_AGENT.MD`) NOW STRICTLY MANDATE Λ-CALCULUS-INSPIRED PRINCIPLES LIKE PARALLELISM (⊗), TOTALITY (∀), ATOMIC EDITS (Δ), AND BOUNDARY SAFETY (∞/0).
- **RUNTIME TOOL CONTRACTS ENFORCED**: `gptel-make-tool` CALLS ARE NOW GLOBALLY WRAPPED WITH A RUNTIME VALIDATOR THAT CHECKS INCOMING ARGUMENTS AGAINST THE DECLARED SCHEMAS. LLM GETS DESCRIPTIVE `user-error` MESSAGES INSTEAD OF CRASHING EMACS.
- **SUBAGENT CONTEXT PACKAGING**: ADDED AUTOMATED CONTEXT INJECTION TO `Agent` AND `RunAgent` TOOLS. SUBAGENTS CAN NOW SEAMLESSLY RECEIVE FILES, GIT DIFFS, AND PARENT CONVERSATION HISTORY VIA OPTIONAL ARGUMENTS (`files`, `include_history`, `include_diff`).
- **PREVIEW TOOLS FSM INTEGRATION FIXED**: PREVIEW TOOLS (`preview_file_change`, `preview_patch`) NOW CORRECTLY CAPTURE AND RESTORE THE `gptel--fsm-last` STATE, PREVENTING THE AGENT FROM HANGING WHEN SWITCHING BUFFERS DURING INTERACTIVE PREVIEWS.
- **LSP SERVER AUTO-START & VERIFICATION**: LSP TOOLS (`lsp_references`, `lsp_definition`, `lsp_hover`, `lsp_rename`) NOW AUTOMATICALLY ATTEMPT TO START THE EGLOT SERVER VIA `eglot-ensure` IF NOT RUNNING. `lsp_diagnostics` NOW STRICTLY VALIDATES ACTIVE PROJECT SERVERS INSTEAD OF RETURNING A SILENT FALSE POSITIVE WHEN NO SERVER IS ACTIVE.
- **UNTRACKED TEST ARTIFACTS CLEANED**: LEFTOVER DEBUGGING FILES FROM THE SANDBOX DEVELOPMENT ITERATIONS HAVE BEEN REMOVED FROM THE REPOSITORY ROOT.
